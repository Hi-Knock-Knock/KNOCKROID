<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.all_the_best.knock_knock">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Knock_Knock">
        <activity android:name=".infant_layout.InfantGiftEndActivity"></activity>
        <activity android:name=".infant_layout.InfantGiftBoxActivity" />
        <activity android:name=".infant_layout.InfantGiftStartActivity" />
        <activity android:name=".infant_layout.InfantSwitchCharacterActivity" />
        <activity android:name=".infant_layout.InfantSelectCharacterActivity" />
        <activity android:name=".infant_layout.InfantTalkStartActivity" />
        <activity android:name=".infant_layout.InfantSelectPersonActivity" />
        <activity android:name=".infant_layout.InfantSelectFeelActivity" />
        <activity android:name=".infant_layout.InfantSelectIdActivity" />
        <activity android:name=".base.SignupActivity" />
        <activity android:name=".base.LoginActivity" />
        <activity android:name=".base.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".base.SelectModeActivity" />
        <activity android:name=".infant_layout.InfantHomeActivity" />
        <activity android:name=".parent_layout.ParentHomeActivity" />
    </application>

</manifest>
